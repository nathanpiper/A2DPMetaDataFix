<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.sweetinsanity.a2dpmetadatafix"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-sdk android:minSdkVersion="7" android:targetSdkVersion="16" />

    <application android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@drawable/ic_launcher"
        android:theme="@style/AppTheme">

        <meta-data android:value="true" android:name="xposedmodule"/>
        <meta-data android:value="2.0*" android:name="xposedminversion"/>
        <meta-data android:value="Provides support for streaming song information from Spotify and TuneIn Radio to a Bluetooth A2DP device." android:name="xposeddescription"/>

    </application>

</manifest>
